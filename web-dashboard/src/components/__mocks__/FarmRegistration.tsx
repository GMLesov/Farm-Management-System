import * as React from 'react';

const FarmRegistration = () => {
  return React.createElement('div', { 'data-testid': 'farm-registration' }, 'Farm Registration');
};

export default FarmRegistration;
